@model IEnumerable<BurstroyMonitoring.Data.Models.VwMueksDataFull>

@{
    ViewData["Title"] = "Данные MUEKS";
}

<div class="view-container">
    <h1 class="view-title">Данные MUEKS</h1>

    <!-- Поиск и фильтры -->
    <div class="search-container">
        <form method="get" class="search-form">
            <div class="search-row">
                <input type="text" name="search" value="@ViewBag.Search" 
                       placeholder="Поиск по серийному номеру, названию точки..." 
                       class="input search-input" />
                <button type="submit" class="button button--primary">Найти</button>
                <a href="@Url.Action("Index")" class="button button--secondary">Сбросить</a>
                <a href="@Url.Action("DataViewer")" class="button button--secondary">Просмотр и экспорт</a>
                <label style="display: inline-flex; align-items: center; gap: 8px; margin-left: 16px; cursor: pointer;">
                    <input type="checkbox" id="autoRefresh" style="width: 16px; height: 16px; margin: 0; accent-color: #0052cc;" />
                    <span style="font-size: 14px; color: #172b4d;">Обновлять автоматически</span>
                </label>
            </div>
        </form>
       
    </div>

    <!-- Пагинация сверху -->
    @await Html.PartialAsync("_Pagination")

    <!-- Таблица данных -->
    <div class="table-responsive">
        <table class="table">
            <thead>
                <tr>
                    <th>
                        <a href="@Url.Action("Index", new { 
                            page = ViewBag.Page, 
                            pageSize = ViewBag.PageSize, 
                            search = ViewBag.Search,
                            sortBy = "MueksDataId",
                            sortDesc = ViewBag.SortBy == "MueksDataId" ? !ViewBag.SortDesc : true
                        })">
                            ID
                            @if (ViewBag.SortBy == "MueksDataId")
                            {
                                <span>@(ViewBag.SortDesc ? "▼" : "▲")</span>
                            }
                        </a>
                    </th>
                    <th>Дата получения</th>
                    <th>Серийный номер</th>
                    <th>Конечная точка</th>
                    <th>Тип датчика</th>
                    <th>Пост</th>
                    <th>Температура</th>
                    <th>Напряжение АКБ</th>
                    <th>Видимость</th>
                    <th>Подробности</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.MueksDataId</td>
                        <td>@item.ReceivedAt?.ToLocalTime().ToString("dd.MM.yyyy HH:mm")</td>
                        <td>@item.SerialNumber</td>
                        <td>@item.EndpointName</td>
                        <td>@item.SensorTypeName</td>
                        <td>
                            @item.PostName
                            @if (item.PostIsMobile == true)
                            {
                                <span class="lozenge" title="Мобильный пост">Моб.</span>
                            }
                        </td>
                        <td>
                            @if (item.TemperatureBox.HasValue)
                            {
                                <span>@item.TemperatureBox.Value.ToString("F1")°C</span>
                            }
                            else
                            {
                                <span class="text-muted">-</span>
                            }
                        </td>
                        <td>
                            @if (item.VoltageAkb.HasValue)
                            {
                                <span>@item.VoltageAkb.Value.ToString("F1")V</span>
                            }
                            else
                            {
                                <span class="text-muted">-</span>
                            }
                        </td>
                        <td>
                            @if (item.VisibleRange.HasValue)
                            {
                                <span>@item.VisibleRange.Value.ToString("F0") м</span>
                            }
                            else
                            {
                                <span class="text-muted">-</span>
                            }
                        </td>
                        <td>
                            <a asp-action="Details" asp-route-id="@item.MueksDataId" 
                               class="button button--sm button--secondary" title="Просмотр деталей">
                                📊
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    <!-- Пагинация снизу -->
    @await Html.PartialAsync("_Pagination")

    <!-- Информация о количестве записей -->
    <div class="records-info">
        Показано @(((ViewBag.Page - 1) * ViewBag.PageSize) + 1)-@Math.Min(ViewBag.Page * ViewBag.PageSize, ViewBag.TotalCount) 
        из @ViewBag.TotalCount записей
    </div>
</div>
