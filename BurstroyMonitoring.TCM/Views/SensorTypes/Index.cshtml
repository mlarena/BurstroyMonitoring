@model IEnumerable<BurstroyMonitoring.Data.Models.SensorType>

@{
    ViewData["Title"] = "Типы датчиков";
    
    // Получаем параметры сортировки из ViewBag
    var sortBy = ViewBag.SortBy as string ?? "Id";
    var sortDesc = ViewBag.SortDesc as bool? ?? true;
    var search = ViewBag.Search as string ?? "";
    
    // Функция для получения URL сортировки
    string GetSortUrl(string columnName)
    {
        var newSortDesc = (sortBy == columnName) ? !sortDesc : true;
        return Url.Action("Index", new { 
            sortBy = columnName, 
            sortDesc = newSortDesc,
            search = search
        });
    }
    
    // Функция для отображения стрелки сортировки
    string GetSortArrow(string columnName)
    {
        if (sortBy == columnName)
        {
            return sortDesc ? "▼" : "▲";
        }
        return "";
    }
}

<div class="view-container">
    <h1 class="view-title">Типы датчиков</h1>
    
    <!-- Поисковая строка -->
    <div class="search-container">
        <form method="get" class="search-form">
            <div class="search-row">
                <input type="text" name="search" value="@search" 
                       placeholder="Поиск по названию типа или описанию..." 
                       class="input search-input" />
                <button type="submit" class="button button--primary">Найти</button>
                @if (!string.IsNullOrEmpty(search))
                {
                    <a href="@Url.Action("Index")" class="button button--secondary">Сбросить</a>
                }
            </div>
            <!-- Скрытые поля для сохранения сортировки -->
            <input type="hidden" name="sortBy" value="@sortBy" />
            <input type="hidden" name="sortDesc" value="@sortDesc.ToString()" />
        </form>
    </div>
    
    <div class="table-header">
        <a asp-action="Create" class="button button--primary">Создать новый</a>
        
        @if (Model.Any())
        {
            <div class="records-count">
                Всего типов: @Model.Count()
                @if (!string.IsNullOrEmpty(search))
                {
                    <span> (по запросу: "@search")</span>
                }
            </div>
        }
    </div>

    <div class="table-responsive">
        <table class="table table--wide">
            <thead>
                <tr>
                    <th>
                        <a href="@GetSortUrl("SensorTypeName")" style="color: inherit; text-decoration: none;">
                            Название типа @GetSortArrow("SensorTypeName")
                        </a>
                    </th>
                    <th>
                        <a href="@GetSortUrl("Description")" style="color: inherit; text-decoration: none;">
                            Описание @GetSortArrow("Description")
                        </a>
                    </th>
                    <th>
                        <a href="@GetSortUrl("Created")" style="color: inherit; text-decoration: none;">
                            Создан @GetSortArrow("Created")
                        </a>
                    </th>
                    <th>
                        <a href="@GetSortUrl("Sensors")" style="color: inherit; text-decoration: none;">
                            Кол-во датчиков @GetSortArrow("Sensors")
                        </a>
                    </th>
                    <th>Действия</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td title="@item.SensorTypeName">
                            @if (item.SensorTypeName.Length > 25)
                            {
                                @item.SensorTypeName.Substring(0, 25)<text>...</text>
                            }
                            else
                            {
                                @item.SensorTypeName
                            }
                        </td>
                        <td class="long-text-cell" title="@item.Description">
                            @if (!string.IsNullOrEmpty(item.Description) && item.Description.Length > 50)
                            {
                                @item.Description.Substring(0, 50)<text>...</text>
                            }
                            else if (!string.IsNullOrEmpty(item.Description))
                            {
                                @item.Description
                            }
                            else
                            {
                                <span class="text-muted">—</span>
                            }
                        </td>
                        <td>
                            @if (item.CreatedAt.HasValue)
                            {
                                <span title="@item.CreatedAt.Value.ToString("dd.MM.yyyy HH:mm:ss")">
                                    @item.CreatedAt.Value.ToString("dd.MM.yyyy HH:mm")
                                </span>
                            }
                            else
                            {
                                <span class="text-muted">—</span>
                            }
                        </td>
                        <td>
                            @if (item.Sensors?.Count > 0)
                            {
                                <span class="sensor-count" title="@item.Sensors.Count датчиков">
                                    @item.Sensors.Count
                                </span>
                            }
                            else
                            {
                                <span class="text-muted">0</span>
                            }
                        </td>
                        <td>
                            <div class="action-buttons">
                                <a asp-action="Edit" asp-route-id="@item.Id" 
                                   class="button button--sm button--secondary" 
                                   title="Редактировать тип">
                                    ✏️
                                </a>
                                <a asp-action="Details" asp-route-id="@item.Id" 
                                   class="button button--sm button--secondary" 
                                   title="Подробности о типе">
                                    📄
                                </a>
                                <a asp-action="Delete" asp-route-id="@item.Id" 
                                   class="button button--sm button--danger" 
                                   title="Удалить тип">
                                    🗑️
                                </a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    
    @if (!Model.Any())
    {
        <div class="empty-state">
            <div class="empty-state-icon">🔧</div>
            <h3>@(string.IsNullOrEmpty(search) ? "Нет типов датчиков" : "Типы датчиков не найдены")</h3>
            <p>@(string.IsNullOrEmpty(search) ? "Создайте первый тип датчика для классификации оборудования" : "Попробуйте изменить параметры поиска")</p>
            <a asp-action="Create" class="button button--primary">Создать тип</a>
            @if (!string.IsNullOrEmpty(search))
            {
                <a href="@Url.Action("Index")" class="button button--secondary" style="margin-left: 10px;">
                    Показать все
                </a>
            }
        </div>
    }
</div>

