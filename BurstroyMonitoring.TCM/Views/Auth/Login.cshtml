@model BurstroyMonitoring.TCM.Controllers.LoginModel

@{
    ViewData["Title"] = "Вход в систему";
    Layout = null; // Отключаем основной макет для страницы входа
}

<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="color-scheme" content="light dark" />
    <title>@ViewData["Title"] - Мониторинг датчиков</title>
    <link rel="stylesheet" href="~/css/site.css" />
    <link rel="stylesheet" href="~/css/password-toggle.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
</head>
<body class="theme-light" style="background: var(--bg-primary);">
    <div class="main-content" style="display: flex; align-items: center; min-height: 100vh; padding-top: 0;">
        <div class="view-container" style="max-width: 450px; margin: 0 auto;">
            <h1 class="view-title text-center">Вход в систему</h1>

            <form asp-action="Login" method="post" class="form-container" style="box-shadow: none; padding: 0;">
               
                <div class="field-group">
                    <label asp-for="UserName" class="control-label required">Имя пользователя</label>
                    <input asp-for="UserName" class="input" placeholder="Введите имя пользователя" />
                    <span asp-validation-for="UserName" class="field-validation-error"></span>
                </div>

                <div class="field-group">
                    <label asp-for="Password" class="control-label required">Пароль</label>
                    <div class="password-toggle">
                        <input asp-for="Password" type="password" class="input" placeholder="Введите пароль" />
                        <button type="button" class="toggle-btn" tabindex="-1">
                            <i class="bi bi-eye-slash"></i>
                        </button>
                    </div>
                    <span asp-validation-for="Password" class="field-validation-error"></span>
                </div>

                <div class="form-actions" style="justify-content: center;">
                    <button type="submit" class="button button--primary" style="min-width: 200px;">Войти</button>
                </div>
            </form>
        </div>
    </div>

    <script src="~/js/lib/jquery.min.js"></script>
    <script src="~/js/lib/jquery.validate.min.js"></script>
    <script src="~/js/lib/jquery.validate.unobtrusive.min.js"></script>
    <script src="~/js/password-toggle.js"></script>
</body>
</html>