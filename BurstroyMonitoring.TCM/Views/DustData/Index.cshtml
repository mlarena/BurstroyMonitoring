@model IEnumerable<BurstroyMonitoring.Data.Models.VwDustDataFull>

@{
    ViewData["Title"] = "Данные DUST";
}

<div class="view-container">
    <h1 class="view-title">Данные DUST</h1>

    <!-- Поиск и фильтры -->
    <div class="search-container">
        <form method="get" class="search-form">
            <div class="search-row">
                <input type="text" name="search" value="@ViewBag.Search" 
                       placeholder="Поиск по серийному номеру, названию точки..." 
                       class="input search-input" />
                <button type="submit" class="button button--primary">Найти</button>
                <a href="@Url.Action("Index")" class="button button--secondary">Сбросить</a>
                <a href="@Url.Action("DataViewer")" class="button button--secondary">Просмотр и экспорт</a>
                <label style="display: inline-flex; align-items: center; gap: 8px; margin-left: 16px; cursor: pointer;">
                    <input type="checkbox" id="autoRefresh" style="width: 16px; height: 16px; margin: 0; accent-color: #0052cc;" />
                    <span style="font-size: 14px; color: #172b4d;">Обновлять автоматически</span>
                </label>
            </div>
        </form>
           

    </div>

    <!-- Пагинация сверху -->
    @await Html.PartialAsync("_Pagination")

    <!-- Таблица данных -->
    <div class="table-responsive">
        <table class="table">
            <thead>
                <tr>
                    <th>
                        <a href="@Url.Action("Index", new { 
                            page = ViewBag.Page, 
                            pageSize = ViewBag.PageSize, 
                            search = ViewBag.Search,
                            sortBy = "DustDataId",
                            sortDesc = ViewBag.SortBy == "DustDataId" ? !ViewBag.SortDesc : true
                        })">
                            ID
                            @if (ViewBag.SortBy == "DustDataId")
                            {
                                <span>@(ViewBag.SortDesc ? "▼" : "▲")</span>
                            }
                        </a>
                    </th>
                    <th>Дата получения</th>
                    <th>Серийный номер</th>
                    <th>Конечная точка</th>
                    <th>PM10</th>
                    <th>PM2.5</th>
                    <th>PM1</th>
                    <th>Температура</th>
                    <th>Влажность</th>
                    <th>Статус лазера</th>
                    <th>Подробности</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.DustDataId</td>
                        <td>@item.ReceivedAt?.ToLocalTime().ToString("dd.MM.yyyy HH:mm")</td>
                        <td>@item.SerialNumber</td>
                        <td>@item.EndpointName</td>
                        <td>
                            @if (item.PM10Act.HasValue)
                            {
                                <span>@item.PM10Act.Value.ToString("F2")</span>
                            }
                            else
                            {
                                <span class="text-muted">-</span>
                            }
                        </td>
                        <td>
                            @if (item.PM25Act.HasValue)
                            {
                                <span>@item.PM25Act.Value.ToString("F2")</span>
                            }
                            else
                            {
                                <span class="text-muted">-</span>
                            }
                        </td>
                        <td>
                            @if (item.PM1Act.HasValue)
                            {
                                <span>@item.PM1Act.Value.ToString("F2")</span>
                            }
                            else
                            {
                                <span class="text-muted">-</span>
                            }
                        </td>
                        <td>
                            @if (item.TemperatureProbe.HasValue)
                            {
                                <span>@item.TemperatureProbe.Value.ToString("F1")°C</span>
                            }
                            else
                            {
                                <span class="text-muted">-</span>
                            }
                        </td>
                        <td>
                            @if (item.HumidityProbe.HasValue)
                            {
                                <span>@item.HumidityProbe.Value.ToString("F1")%</span>
                            }
                            else
                            {
                                <span class="text-muted">-</span>
                            }
                        </td>
                        <td>
                            @if (item.LaserStatus.HasValue)
                            {
                                @if (item.LaserStatus > 0)
                                {
                                    <span class="lozenge lozenge--success" title="Статус лазера: @item.LaserStatus">✓</span>
                                }
                                else
                                {
                                    <span class="lozenge lozenge--error" title="Статус лазера: @item.LaserStatus">✗</span>
                                }
                            }
                            else
                            {
                                <span class="text-muted">-</span>
                            }
                        </td>
                        <td>
                            <a asp-action="Details" asp-route-id="@item.DustDataId" 
                               class="button button--sm button--secondary" title="Просмотр деталей">
                                📊
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    <!-- Пагинация снизу -->
    @await Html.PartialAsync("_Pagination")

    <!-- Информация о количестве записей -->
    <div class="records-info">
        Показано @(((ViewBag.Page - 1) * ViewBag.PageSize) + 1)-@Math.Min(ViewBag.Page * ViewBag.PageSize, ViewBag.TotalCount) 
        из @ViewBag.TotalCount записей
    </div>
</div>
